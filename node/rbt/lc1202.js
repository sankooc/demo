
class Dob {
  constructor(a, b){
    this.a = a;
    this.b = b;
  }
}

const smallestStringWithSwaps = (s, pairs) => {
  let i = 0;
  const map = {};
  const inc = (v1, v2) => {
    const t = map[v1] || map[v2];
    if (t) {
    }
  };
  for (const p of pairs) {
    
    p[0];
    p[1];
  }
};1


console.dir(smallestStringWithSwaps('', [[0, 1], [3, 2], [5, 7], [1, 3]]));
