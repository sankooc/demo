ALTER TABLE INVITOR_COMPANY RENAME TO EMPLOYEE;

ALTER TABLE EMPLOYEE ADD AT timestamp default now();

CREATE TABLE VFILE(
  URL TEXT,
  MD5 VARCHAR(40),
  SIZE INTEGER,
  MIME TEXT,
  EXT VARCHAR(10),
  META JSON,
  UNIQUE (MD5, SIZE)
);

ALTER TABLE OFFICE ADD NEED_AUTH BOOLEAN DEFAULT TRUE;